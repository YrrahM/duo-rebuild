// next.config.js
/// <reference types="next" />
/** @type {import('next').NextConfig} */
const nextConfig = {
  // keep anything else you actually use; just no "i18n" here
};

export default nextConfig;
